# Handoff — 4 August 2026

Owner: Bailey. Covers the email/form system check, the SEO/AEO/GEO audit, everything shipped, and the roadmap. Two deploys today: `d204c37` (email) and `97d12b6` (SEO/AEO).

---

## 1. Email + form system — checked end to end (Hazem's ask)

**Verdict: it works.** The "whole mess glued together" is resolved — the system is properly wired now.

- Prod (Vercel `rosey-co-team/simon-jacobs`) has `RESEND_API_KEY`, `CRON_SECRET`, `SUPABASE_SERVICE_ROLE_KEY`, admin vars — all set (stored Sensitive, so they read blank on `env pull`, but are live).
- `srjinternational.co.uk` is verified in the RoseyCo Resend account; live test send delivered.
- Supabase healthy: `leads`, `email_queue`, `suppressed_emails`, `blog_posts`(54), `login_attempts` all present.
- Forms (contact / subscribe / scorecard) → `/api/contact` → Supabase insert + Simon notify + welcome sequence (call + scorecard tracks, Email 1 instant, 2–3 via daily cron). All coded and wired.
- Why it looked dead: only 1 lead ever (a launch-day test). Traffic, not wiring.

### Shipped (`d204c37`): lead alerts also go to `team@elevateoco.com` — redacted
- `team@elevateoco.com` added alongside `hazem.dweik@` as a recipient of every lead alert.
- The **Elevateo copy is minimised for GDPR**: email + phone masked, free-text enquiry withheld, enquirer reply-to stripped. Only Simon (data controller) gets raw personal data by email; full detail stays behind `/admin`. FormSubmit fallback goes to Simon only.
- **Action for Hazem:** Simon's privacy policy should mention Elevateo (processor) handling enquiry data; a DPA between Simon and Elevateo covers the contractual side.

### Not a mailing list (Hazem asked)
Subscribers are captured into `leads` (`source=subscribe`) but receive nothing automated. The only auto-emails are the triggered nurture sequences (qualified call leads + scorecard finishers). No Resend Audience / broadcast wiring exists. To make it a true list: wire subscribers into a Resend Audience + Broadcasts (not built — say the word).

---

## 2. SEO / AEO / GEO audit + fixes

Full audit against the SEO-Resources frameworks (master playbook, GEO/AEO/LLMO 2026 research, E-E-A-T, topical maps) across 4 dimensions. Full reports archived (technical / aeo-geo / schema / content-eeat).

**Verdict: already strong** — ~85% of a serious AEO plan shipped; no P0 technical defects. The gaps were entity consolidation, on-page answer furniture, and search-engine visibility.

### Shipped (`97d12b6`)
- **Entity unification (biggest win):** Simon was fragmented across the site — homepage founder, `/about`, and all 23 blog authors each emitted a *separate* `Person`. Now every mention references one canonical `@id` node (`/#simon-jacobs`, defined on `/about`), with machine-readable `hasCredential` (CTA·CIOT, ACA·ICAEW). `@id` graph verified consistent (org `/#organization`, WebSite `/#website`).
- **On-page E-E-A-T (was schema-only):** visible "By Simon Jacobs, CTA·ACA" byline + "In short:" TL;DR callout + author bio card on every blog post. Verified live.
- **GSC verification (was a hidden P0):** domain was never in Search Console (GoDaddy DNS blocked DNS-TXT; consent-gated GA4 defeats the ANALYTICS method). Verified via the **FILE method** (`public/google6c79a1949b8634fa.html`); property `https://srjinternational.co.uk/` added, sitemap submitted. **Search data starts flowing now.**
- **Schema:** added `WebSite` node (no SearchAction) + `Service` schema on `/services`.
- **Technical:** robots (dropped non-existent `Claude-Web`, added `CCBot`), sitemap `lastModified` on static routes, claude gif lazy-load.

### Needs Simon (do NOT fabricate)
- Testimonials → `Review`/`AggregateRating` (confirm authenticity + resolve Joe G / Hamish B attribution conflicts + rating sources).
- Principal-vs-agent / ad-spend passthrough VAT stance (his missing "moat" topic).
- CIOT "Find a CTA" register URL — `TODO(verify)` left in `about/page.tsx` `sameAs`.

### Off-site — the real AEO ceiling (not code)
AI engines / Google won't *name* SRJ until there's off-site corroboration: **CIOT register, ICAEW listing, Google Business Profile + reviews** (rivals have 100–130; SRJ ~0). Highest-leverage growth lever; Simon/Hazem action.

### Next code pass (all safe, code-only)
- Blog renderer: support lists + tables (GEO extraction win).
- Internal linking: make bidirectional; fix orphaned London page (7 posts have 0 internal links, only 8/23 link up to pillars).
- Dynamic `llms.txt` (drifted: 14 listed vs 23 live).
- OG image 1200×630 (currently 961² square, crops on every share).
- www→apex 301.
- Content: principal-vs-agent VAT pillar + `/accountants-for-media-agencies`; rein in scheduled-post topic drift off the agency niche.

---

## 3. State of play
- Repo at `97d12b6` on `master`. Two clean deploys today, both live and verified.
- GA4 `G-FJGM7PLZEC` (+ second `G-6S1EHH7C90`) live and consent-gated. **GSC now verified** (new).
- Client-facing report: `Elevateo-Docs/roseyco/client-facing/` (SRJ SEO/AEO/GEO Audit + Growth Plan).
